# knok-build-macros

`knok-build-macros` provides the procedural macros used by `knok-build` to
register build-time graph functions.

Most users should depend on `knok-build`, which re-exports the public macro
entry points and tracing prelude.

Repository and user documentation:

https://github.com/gmmyung/knok
