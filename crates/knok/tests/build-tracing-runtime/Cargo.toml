[package]
name = "knok-build-tracing-runtime"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
knok = { path = "../.." }

[build-dependencies]
knok-build = { path = "../../../knok-build" }
