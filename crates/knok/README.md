# knok

`knok` is the public runtime and tensor crate for the knok workspace.

It provides fixed-rank tensor containers, graph artifacts, runtime configuration,
and generated graph import helpers. Build-time graph compilation is provided by
the companion `knok-build` crate.

See the repository README for setup, examples, feature flags, and current
limitations:

https://github.com/gmmyung/knok
